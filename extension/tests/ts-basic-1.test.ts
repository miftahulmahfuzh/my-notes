/**
 * Minimal TypeScript test - Step 1
 */

test('basic JavaScript syntax works', () => {
  const message = 'Hello World';
  const number = 42;

  expect(message).toBe('Hello World');
  expect(number).toBe(42);
});